# Copy this file to `.env.development` (for local dev) and set real values.
# DO NOT commit secrets. For Production, set the variables in your hosting provider (Vercel).

# ==================== Backend API ====================
# Base URL of the backend. Must be prefixed with VITE_ to be exposed to the Vite client.
# Development:
VITE_API_BASE_URL=http://localhost:5000

# Production (set in Vercel Environment Variables):
# VITE_API_BASE_URL=https://your-backend-domain.vercel.app

# ==================== Payment Integration ====================

# Stripe Publishable Key
# Get from: https://dashboard.stripe.com > Developers > API keys
# Copy the "Publishable key" (starts with pk_test_ for sandbox, pk_live_ for production)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# PayPal Client ID
# Get from: https://developer.paypal.com > Dashboard > Apps & Credentials
# Select Sandbox mode for testing, Live for production
VITE_PAYPAL_CLIENT_ID=your_sandbox_client_id_here

# ==================== Important ====================
# Any client-side env variables must start with VITE_
# These will be exposed to the frontend at build time
